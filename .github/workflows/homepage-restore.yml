name: be.up homepage deployment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow
        run: echo 'Deprecated: direct site source is authoritative; no restore job runs automatically.'
