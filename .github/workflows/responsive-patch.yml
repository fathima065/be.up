name: be.up responsive patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow
        run: echo 'Deprecated: homepage-restore.yml is the only active site patch workflow.'
